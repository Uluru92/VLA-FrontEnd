notas angular

npm init   === iniciamos proyecto y creamos el archivo package.json
npm run dev === inicializamos la API
ng serve  === inicializamos el localhost
ng version === revisamos la version
node -v   === revisamos version de node
npm install === instalamos node_modules
npm update  === actualizamos todas las dependencias
npm install bootstrap === instalamos bootstrap principal
npm install @ng-bootstrap/ng-bootstrap@17.0.1 --legacy-peer-deps === instalamos boostra tropicalizado segun version
ng generate service consumo-api-pais ===
ng generate component General       ===  generamos un componente
npm i @angular/core === instala ultima version "@angular/core": "^18.2.11" la version mas importante a actualizadar
npm i @ng-bootstrap/ng-bootstrap === instala la ultima version tropicalizada


pagina documentacion ng bootstrap: 
-ng-bootstrap.github.it/#/home

pagina para bugs: stackoverflow.com
pagina de recursos botones: 
-bootstrap
-material framework design
-tailwindcss.com


notas REACT

node - v === para confirmar que version de node estamos usando y sea todo compatible
npm install -g create-react-app === instalando react soporte de react 
npx create-react-app mi-primer-app === crear un nuevo proytecto
npm start === levanta inicializamos el localhost
npm install bootstrap === instalamos bootstrap
npm install react-router-dom === ***instalar al puro inicio después de crear el proyecto
npm i -D react-router-dom@latest === ***instalar la ultima version de react router
npm install --save typescript @types/react @types/react-dom ===
create-react-app laboratorio-jorddy --template typescript === esto es para crear un proyecto nuevo default ts
npm i react-bootstrap === instalar componente react bootstrap
npm installa axios === para consumo de las apis
npm i react-scripts 
npm install react-scripts@latest === instalar ultima version de react-scripts
https://developer.mozilla.org/es/docs/Web/API/Window/clearInterval === documentacion HTML intervalos etc
npm run build === construccion de los compilados...