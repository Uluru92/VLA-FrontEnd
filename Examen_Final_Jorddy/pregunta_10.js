var a = null; //declaramos una variable explicitamente vacía
console.log(a); //obtenemos null

var b;  // declaramos una variable pero no le asignamos un valor, es decir no se inicializa
console.log(b); //obtenemos undefined
